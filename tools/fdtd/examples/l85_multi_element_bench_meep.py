# Generated by EMMicro L8.1 External FDTD / Field Maps.
# Optional external helper only; the browser app does not execute FDTD.
# source_scenario_hash: 2569f7e424c2366a
# manifest_hash: dcc211305c62b196

import json
import meep as mp

cell_size = mp.Vector3(24, 18, 65000)
resolution = 24
pml_layers = [mp.PML(0.75)]

materials = {
    "air": mp.Medium(index=1),  # k=0 hash=2653810c8f22d751
    "target-dielectric": mp.Medium(index=1.5),  # k=0 hash=fee8aa9aed270deb
    "element-finite-transparent-block-30000-dielectric": mp.Medium(index=1.5),  # k=0 hash=885cb9c0297dc58e
    "element-finite-absorbing-block-40000-absorber": mp.Medium(index=1.2),  # k=0.02 hash=3fee2e989dcfd0a2
}

geometry = [
    mp.Block(center=mp.Vector3(0, 0, 50000), size=mp.Vector3(24, 18, 25), material=materials["target-dielectric"]),  # transparent-slab
    mp.Block(center=mp.Vector3(0, 0, 30000), size=mp.Vector3(8, 8, 5), material=materials["element-finite-transparent-block-30000-dielectric"]),  # finite-transparent-block
    mp.Block(center=mp.Vector3(0, 0, 40000), size=mp.Vector3(8, 8, 100), material=materials["element-finite-absorbing-block-40000-absorber"]),  # finite-absorbing-block
]

sources = [
    mp.Source(mp.ContinuousSource(wavelength=0.5), component=mp.Ez, center=mp.Vector3(0, 0, 0), size=mp.Vector3(24, 18, 0)),
]

sim = mp.Simulation(cell_size=cell_size, boundary_layers=pml_layers, geometry=geometry, sources=sources, resolution=resolution)

# Monitor definitions. External postprocess writes flux_summary.json and field_slice_xz.csv.
# monitor incident-flux: flux-plane center=(0, 0, 49500) size=(24, 18, 0) normal=+z
# monitor reflected-flux: flux-plane center=(0, 0, 49200) size=(24, 18, 0) normal=-z
# monitor transmitted-flux: flux-plane center=(0, 0, 50800) size=(24, 18, 0) normal=+z
# monitor field-slice-xz: field-slice center=(0, 0, 32500) size=(24, 0, 65000) normal=+y
# monitor finite-transparent-block-30000-front-flux: flux-plane center=(0, 0, 29992.5) size=(8, 8, 0) normal=+z
# monitor finite-transparent-block-30000-back-flux: flux-plane center=(0, 0, 30007.5) size=(8, 8, 0) normal=+z
# monitor finite-absorbing-block-40000-front-flux: flux-plane center=(0, 0, 39850) size=(8, 8, 0) normal=+z
# monitor finite-absorbing-block-40000-back-flux: flux-plane center=(0, 0, 40150) size=(8, 8, 0) normal=+z

# sim.run(until=200)
# sim.output_efield_z()
# Use tools/fdtd/postprocess_meep_output.py to produce importable JSON/CSV summaries.
